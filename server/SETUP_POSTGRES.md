# Настройка PostgreSQL

## Установка PostgreSQL

### Windows

1. Скачайте установщик с [официального сайта](https://www.postgresql.org/download/windows/)
2. Запустите установщик и следуйте инструкциям
3. Запомните пароль для пользователя `postgres` (будет использоваться при установке)
4. Порт по умолчанию: `5432`

### Linux (Ubuntu/Debian)

```bash
sudo apt update
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

### macOS

```bash
brew install postgresql@15
brew services start postgresql@15
```

## Создание базы данных

### Способ 1: Через psql (командная строка)

1. Откройте терминал/командную строку
2. Подключитесь к PostgreSQL:

**Windows:**
```bash
psql -U postgres
```

**Linux/macOS:**
```bash
sudo -u postgres psql
```

3. Создайте базу данных:
```sql
CREATE DATABASE supporit;
```

4. Выйдите из psql:
```sql
\q
```

### Способ 2: Через pgAdmin (графический интерфейс)

1. Откройте pgAdmin
2. Подключитесь к серверу PostgreSQL
3. Правой кнопкой на "Databases" → "Create" → "Database"
4. Имя: `supporit`
5. Нажмите "Save"

## Применение схемы базы данных

После создания базы данных примените схему:

**Вариант 1: С использованием sudo (Linux/macOS)**
```bash
# Из корня проекта
sudo -u postgres psql -d supporit -f /полный/путь/к/проекту/supabase/schema_postgres.sql
```

**Вариант 2: С использованием пароля (если нет доступа к sudo)**
```bash
# Из корня проекта
PGPASSWORD='ваш_пароль' psql -U postgres -h localhost -d supporit -f supabase/schema_postgres.sql
```

**Вариант 3: Через psql (интерактивно)**
```bash
# Подключитесь к базе данных
sudo -u postgres psql -d supporit
# Или с паролем:
psql -U postgres -h localhost -d supporit
```

Затем выполните:
```sql
\i /полный/путь/к/проекту/supabase/schema_postgres.sql
\q
```

**Примечание:** Если возникает ошибка "Отказано в доступе" при использовании `sudo -u postgres`, используйте вариант 2 с переменной окружения `PGPASSWORD` или укажите полный абсолютный путь к файлу.

## Настройка переменных окружения

Создайте файл `server/.env`:

```env
DATABASE_URL=postgresql://postgres:ваш_пароль@localhost:5432/supporit
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
```

**Важно:** Замените:
- `postgres` на ваше имя пользователя PostgreSQL (если отличается)
- `ваш_пароль` на ваш пароль PostgreSQL
- `5432` на ваш порт PostgreSQL (если отличается)

## Проверка подключения

### Проверка через psql

```bash
psql -U postgres -d supporit
```

Если подключение успешно, вы увидите приглашение:
```
supporit=#
```

### Проверка через Node.js

Запустите сервер:
```bash
cd server
npm run dev
```

Если все настроено правильно, вы увидите:
```
✅ Подключение к PostgreSQL установлено
🚀 Сервер запущен на порту 3001
```

## Решение проблем

### Ошибка: "connect ECONNREFUSED"

**Причина:** PostgreSQL не запущен или недоступен

**Решение:**
1. Проверьте, запущен ли PostgreSQL:
   - Windows: Откройте "Службы" и найдите "postgresql-x64-XX"
   - Linux: `sudo systemctl status postgresql`
   - macOS: `brew services list`

2. Запустите PostgreSQL:
   - Windows: Запустите службу через "Службы"
   - Linux: `sudo systemctl start postgresql`
   - macOS: `brew services start postgresql@15`

### Ошибка: "Peer authentication failed for user postgres"

**Причина:** На Linux PostgreSQL по умолчанию использует peer authentication, которая требует, чтобы системный пользователь совпадал с пользователем PostgreSQL.

**Решение:**

**Вариант 1 (рекомендуется):** Используйте `sudo` для переключения на пользователя postgres:
```bash
sudo -u postgres psql
```

**Вариант 2:** Используйте TCP/IP подключение с паролем:
```bash
psql -U postgres -h localhost
```
Вам будет предложено ввести пароль пользователя postgres.

**Вариант 3:** Если вы не знаете пароль, установите его через sudo:
```bash
sudo -u postgres psql -c "ALTER USER postgres PASSWORD 'ваш_новый_пароль';"
```

**Вариант 4:** Создайте пользователя PostgreSQL с именем вашего системного пользователя:
```bash
# Узнайте ваше имя пользователя
whoami

# Создайте пользователя PostgreSQL (требует sudo)
sudo -u postgres createuser -s $USER
# Теперь вы можете подключаться без указания -U
psql -d postgres
```

### Ошибка: "password authentication failed"

**Причина:** Неправильный пароль в DATABASE_URL

**Решение:**
1. Проверьте пароль в `.env` файле
2. Или сбросьте пароль:
```sql
ALTER USER postgres PASSWORD 'a31337Zy77';
```

### Ошибка: "database does not exist"

**Причина:** База данных не создана

**Решение:**
```sql
CREATE DATABASE supporit;
```

### Ошибка: "relation does not exist"

**Причина:** Схема базы данных не применена

**Решение:**
```bash
psql -U postgres -d supporit -f supabase/schema_postgres.sql
```

### Ошибка: "Отказано в доступе" при применении схемы

**Причина:** Пользователь `postgres` не может прочитать файл в вашей домашней директории при использовании `sudo -u postgres`.

**Решение:**

**Вариант 1:** Используйте TCP/IP подключение с паролем:
```bash
PGPASSWORD='ваш_пароль' psql -U postgres -h localhost -d supporit -f supabase/schema_postgres.sql
```

**Вариант 2:** Используйте абсолютный путь к файлу:
```bash
sudo -u postgres psql -d supporit -f /полный/путь/к/проекту/supabase/schema_postgres.sql
```

**Вариант 3:** Скопируйте файл во временную директорию:
```bash
cp supabase/schema_postgres.sql /tmp/
sudo -u postgres psql -d supporit -f /tmp/schema_postgres.sql
rm /tmp/schema_postgres.sql
```

## Альтернатива: Использование Docker

Если не хотите устанавливать PostgreSQL локально, можно использовать Docker:

```bash
# Запуск PostgreSQL в Docker
docker run --name supporit-postgres \
  -e POSTGRES_PASSWORD=postgres \
  -e POSTGRES_DB=supporit \
  -p 5432:5432 \
  -d postgres:15

# Применение схемы
docker exec -i supporit-postgres psql -U postgres -d supporit < supabase/schema_postgres.sql
```

Тогда DATABASE_URL будет:
```env
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/supporit
```

