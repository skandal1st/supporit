# Система учета ИТ-оборудования

Веб-приложение для учета ИТ-оборудования на предприятии с использованием React, TypeScript, Tailwind CSS, Express и PostgreSQL.

## 📚 Документация

### Развертывание
- **[PRODUCTION_DEPLOYMENT.md](PRODUCTION_DEPLOYMENT.md)** - 🚀 Полное руководство по развертыванию на продакшн Ubuntu сервере
- **[DEPLOYMENT.md](DEPLOYMENT.md)** - Общее руководство по развертыванию на новом сервере
- **[scripts/README.md](scripts/README.md)** - Документация по скриптам развертывания

### Настройка
- **[SETUP_ADMIN.md](SETUP_ADMIN.md)** - Создание первого администратора
- **[server/SETUP_POSTGRES.md](server/SETUP_POSTGRES.md)** - Подробная настройка PostgreSQL
- **[MIGRATION_GUIDE.md](MIGRATION_GUIDE.md)** - Руководство по миграции с Supabase

### Безопасность
- **[SECURITY.md](SECURITY.md)** - ⚠️ Руководство по безопасности для продакшена

## Функциональность

### ✅ Реализовано

- 🔐 Аутентификация пользователей
- 👥 Система ролей (Администратор, ИТ-специалист, Обычный сотрудник)
- 📊 Dashboard с основной статистикой
- 🎨 Современный адаптивный UI с темной/светлой темой
- 📱 Мобильная версия с боковым меню

### 🚧 В разработке

- Учет оборудования (CRUD операции)
- Система заявок
- QR-коды для оборудования
- Учет расходных материалов
- Учет лицензий ПО
- Отчеты и аналитика
- База знаний
- Уведомления

## Технологический стек

- **Frontend**: React 19 + TypeScript
- **Стилизация**: Tailwind CSS
- **State Management**: Zustand
- **Роутинг**: React Router v7
- **Формы**: React Hook Form + Zod
- **Таблицы**: TanStack Table
- **Графики**: Recharts
- **Иконки**: Lucide React
- **Дата**: date-fns
- **Backend**: Express + TypeScript
- **База данных**: PostgreSQL
- **Аутентификация**: JWT токены
- **QR-коды**: qrcode.react

## Установка и запуск

> 💡 **Для развертывания на новом сервере:** См. подробное руководство [`DEPLOYMENT.md`](DEPLOYMENT.md)

### Быстрый старт (разработка)

### 1. Клонирование и установка зависимостей

```bash
# Установка зависимостей фронтенда
npm install

# Установка зависимостей бэкенда
cd server
npm install
cd ..
```

### 2. Установка и настройка PostgreSQL

**Если PostgreSQL не установлен:**

- **Windows:** Скачайте с [официального сайта](https://www.postgresql.org/download/windows/)
- **Linux:** `sudo apt install postgresql postgresql-contrib`
- **macOS:** `brew install postgresql@15`

**Подробная инструкция:** См. `server/SETUP_POSTGRES.md`

**Создание базы данных:**

1. Откройте терминал и подключитесь к PostgreSQL:

**Windows:**
```bash
psql -U postgres
```

**Linux/macOS:**
```bash
sudo -u postgres psql
```

**Примечание:** Если на Linux возникает ошибка "Peer authentication failed", см. раздел "Решение проблем" в `server/SETUP_POSTGRES.md`

2. Создайте базу данных:
```sql
CREATE DATABASE supporit;
\q
```

3. Примените схему:

**Windows:**
```bash
psql -U postgres -d supporit -f supabase/schema_postgres.sql
```

**Linux/macOS (с sudo):**
```bash
sudo -u postgres psql -d supporit -f /полный/путь/к/проекту/supabase/schema_postgres.sql
```

**Linux/macOS (без sudo, с паролем):**
```bash
PGPASSWORD='ваш_пароль' psql -U postgres -h localhost -d supporit -f supabase/schema_postgres.sql
```

**Примечание:** Если возникает ошибка "Отказано в доступе" при использовании sudo, используйте вариант с `PGPASSWORD` или см. подробности в `server/SETUP_POSTGRES.md`

**Важно:** Убедитесь, что PostgreSQL запущен перед запуском сервера!

### 3. Настройка переменных окружения

**Бэкенд** (создайте `server/.env`):
```env
DATABASE_URL=postgresql://user:password@localhost:5432/supporit
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
```

**Фронтенд** (создайте `.env` в корне проекта):
```env
VITE_API_URL=http://localhost:3001/api
```

### 4. Запуск приложения

**Запуск бэкенда:**
```bash
cd server
npm run dev
```

Сервер будет доступен на `http://localhost:3001`

**Запуск фронтенда:**
```bash
npm run dev
```

Приложение будет доступно на `http://localhost:5173`

### 5. Создание первого администратора

После запуска приложения:

1. Зарегистрируйтесь через форму регистрации на странице `/register`
2. Подключитесь к базе данных и выполните SQL-запрос (замените `your-email@example.com` на ваш email):

```sql
UPDATE users
SET role = 'admin'
WHERE email = 'skandal1inc@gmail.com';

```

**Важно:** По умолчанию все новые пользователи получают роль `employee`. Только администратор может изменять роли других пользователей.

---

**Примечание:** Полная схема базы данных находится в файле `supabase/schema_postgres.sql`. Используйте его для настройки базы данных.

**Важно:** После применения схемы необходимо применить миграцию для разрешения NULL значений в `password_hash`:

```bash
psql -U postgres -d supporit -f supabase/migration_allow_null_password.sql
```

Это позволит администраторам создавать пользователей без пароля (пароль можно установить при первом входе).

### 6. Запуск приложения

**Режим разработки:**
```bash
# В одном терминале - бэкенд
cd server
npm run dev

# В другом терминале - фронтенд
npm run dev
```

**Сборка для продакшена:**
```bash
# Сборка бэкенда
cd server
npm run build
npm start

# Сборка фронтенда
npm run build
npm run preview
```

## Структура проекта

```
supporit/
├── server/              # Бэкенд API сервер
│   ├── src/
│   │   ├── config/     # Конфигурация БД
│   │   ├── middleware/ # Middleware (auth)
│   │   ├── routes/     # API маршруты
│   │   └── index.ts    # Точка входа
│   └── package.json
├── src/                # Frontend
│   ├── components/     # Переиспользуемые компоненты
│   │   ├── auth/       # Компоненты аутентификации
│   │   └── layout/     # Компоненты layout
│   ├── pages/          # Страницы приложения
│   ├── lib/            # HTTP клиент (api.ts)
│   ├── services/       # API сервисы
│   ├── store/          # Zustand stores
│   ├── types/          # TypeScript типы
│   └── utils/          # Вспомогательные функции
└── supabase/
    └── schema_postgres.sql  # Схема БД
```

## Роли пользователей

### Администратор (admin)
- Полный доступ ко всем функциям
- Управление пользователями
- Управление оборудованием
- Управление заявками
- Просмотр всех отчетов

### ИТ-специалист (it_specialist)
- Управление заявками
- Просмотр и управление оборудованием
- Просмотр отчетов
- Работа с базой знаний

### Обычный сотрудник (employee)
- Создание заявок
- Просмотр своего оборудования
- Просмотр статуса своих заявок
- Доступ к базе знаний

## Следующие шаги разработки

1. ✅ Аутентификация и роли
2. 🔄 Базовый учет оборудования (CRUD)
3. ⏳ Система заявок
4. ⏳ Dashboard и базовая аналитика
5. ⏳ Остальные модули

## Лицензия

MIT
